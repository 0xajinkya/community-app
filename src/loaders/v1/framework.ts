import cookieSession from "cookie-session";
import express, { Application } from "express";
import { Env } from "./env";

export const FrameworkLoader = (app: Application) => {
  // app.use(express.json());
  // app.use(express.urlencoded({ limit: "10mb", extended: true }));
};
